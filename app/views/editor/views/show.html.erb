<% content_for :title, @view.title %>
<% content_for :title_svg do %>
  <i class="bi bi-file-spreadsheet"></i>
<% end %>

<%= render View::EditableListComponent.new(view: @view, items: @items, query: params[:q], pagy: @pagy) %>
<%= render Item::EditOffCanvasComponent.new %>
