<%= turbo_frame_tag "page_handler" do %>
  <%= turbo_stream_action_tag("append", target: "items", template: %(#{render partial: "#{@tab}_item", collection: @items, as: :item })) %>
  <%= turbo_stream_action_tag("replace", target: "load-more", template: %(#{render "load_more", pagy: @pagy, tab: @tab})) %>
<% end %>